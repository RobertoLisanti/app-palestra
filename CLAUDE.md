# app-palestra (AndyGym)

PWA schede di allenamento. Repo **pubblico** `RobertoLisanti/app-palestra`,
live https://robertolisanti.github.io/app-palestra/ (GitHub Pages, `main`/root, auto-deploy al push).

**Architettura (come)** -> skill `sito-web-statico` + `backend-supabase` (nel repo di config,
`.claude\skills\`).

Questo repo e' **pubblico**: i fatti specifici del progetto (chiavi, ref Supabase, schema,
nomi utenti reali) e lo stato "fatto / da fare" NON stanno qui ma in un file locale, mai
committato (vedi `.gitignore`):

@.claude/local.md
